import 'package:flutter/material.dart';

class TrafficUpdate extends StatefulWidget {
  const TrafficUpdate({super.key});

  @override
  State<TrafficUpdate> createState() => _TrafficUpdateState();
}

class _TrafficUpdateState extends State<TrafficUpdate> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
